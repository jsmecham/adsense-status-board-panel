
%header
 
  %h1 Google AdSense Revenue Widgets

%main

  %section.revenues
    %header
      .widgets
        - [ :today, :this_month, :last_month ].each do |period|
          .widget-container
            %iframe{ src: "earnings/#{period}" }
            %button{ onclick: "location.href='panicboard://?url=#{CGI.escape("#{settings.base_url}/earnings/#{period}?token=#{current_user.access_token}")}&panel=diy&sourceDisplayName=#{CGI.escape("AdSense Status Board Widgets")}'" }
              Add to Status Board
